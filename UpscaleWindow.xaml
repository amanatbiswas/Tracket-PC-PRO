<Window
    x:Class="App.UpscaleWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TRACKET AI // TOPAZ UPSCALE ENGINE">

    <!-- LIQUID GLASS BACKDROP (WinUI 3 Desktop Acrylic) -->
    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <!-- Semi-transparent Phonk overlay to let the glass shine through -->
    <Grid Background="#7007090E" Padding="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="AI VIDEO UPSCALING &amp; ENHANCEMENT" FontSize="18" FontWeight="Bold" Foreground="#00E5FF" CharacterSpacing="100"/>
            <TextBlock Text="Powered by NVIDIA CUDA / Neural Acceleration" FontSize="12" Foreground="#94A3B8"/>
        </StackPanel>

        <!-- Glassmorphic Controls Panel -->
        <Border Grid.Row="1" Margin="0,20" Background="#400D111A" BorderBrush="#4000E5FF" BorderThickness="1" CornerRadius="12" Padding="25">
            <StackPanel VerticalAlignment="Center" Spacing="22">

                <StackPanel Spacing="6">
                    <TextBlock Text="TARGET RESOLUTION OUTPUT" FontSize="11" Foreground="#CBD5E1" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CmbResolution" HorizontalAlignment="Stretch" Background="#8007090E" Foreground="White" SelectedIndex="1" BorderBrush="#1E293B">
                        <ComboBoxItem Content="2K (1440p) - High Clarity"/>
                        <ComboBoxItem Content="4K (2160p) - Ultra Cinematic"/>
                        <ComboBoxItem Content="8K (4320p) - Extreme Render"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Spacing="6">
                    <TextBlock Text="AI NEURAL MODEL" FontSize="11" Foreground="#CBD5E1" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CmbAIModel" HorizontalAlignment="Stretch" Background="#8007090E" Foreground="White" SelectedIndex="0" BorderBrush="#1E293B">
                        <ComboBoxItem Content="EDSR (Enhanced Deep Residual) - Best for Quality"/>
                        <ComboBoxItem Content="FSRCNN (Fast Super Resolution) - Best for Speed"/>
                        <ComboBoxItem Content="ESPCN (Real-Time Video) - High FPS"/>
                    </ComboBox>
                </StackPanel>

                <!-- Hardware GPU Acceleration -->
                <CheckBox x:Name="ChkGpuAccel" Content="Force Dedicated NVIDIA CUDA GPU Acceleration" IsChecked="True" Foreground="#00E5FF" FontSize="12"/>

                <!-- LIVE PROGRESS & METRICS DASHBOARD (Hidden until start) -->
                <StackPanel x:Name="ProgressDashboard" Visibility="Collapsed" Spacing="8" Margin="0,15,0,0">
                    <TextBlock Text="NEURAL RENDERING PIPELINE ACTIVE" FontSize="11" Foreground="#00E5FF" FontWeight="Bold" CharacterSpacing="50"/>
                    <ProgressBar x:Name="RenderProgressBar" Minimum="0" Maximum="100" Height="6" Foreground="#00E5FF" Background="#1E293B" CornerRadius="3"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="ProgressText" Text="Frame: 0 / 0 (0%)" FontSize="11" Foreground="#94A3B8"/>
                        <TextBlock x:Name="FpsText" Grid.Column="1" Text="GPU Speed: 0.0 FPS" FontSize="11" Foreground="#00E5FF" FontWeight="SemiBold"/>
                    </Grid>
                    <TextBlock x:Name="TimeText" Text="Time Remaining: Calculating..." FontSize="11" Foreground="#64748B" HorizontalAlignment="Right"/>
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- Bottom Action Deck -->
        <Grid Grid.Row="2">
            <Button x:Name="BtnStartUpscale" Content="START AI RENDER" Background="#00E5FF" Foreground="#07090E" FontWeight="Bold" HorizontalAlignment="Stretch" Height="45" CornerRadius="6" Click="BtnStartUpscale_Click"/>
        </Grid>
    </Grid>
</Window>